import React from 'react';
import BlockUser from '../components/user/BlockUser';

const BlockUserScreen = ({navigation}) => {
  return <BlockUser navigation={navigation} />;
};

export default BlockUserScreen;
